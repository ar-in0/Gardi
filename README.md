# Quickstart
``` shell
# install
git clone https://github.com/ar-in0/Gardi.git
pip install -e . # editable mode, source code changes reflected in subseuent runs

# run
gardi --debug # enable debug logs
```
