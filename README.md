# Quickstart
``` shell
# Obtain source files
git clone https://github.com/ar-in0/Gardi.git

cd Gardi

# Create venv
python3 -m venv gardi-venv
source gardi-venv/bin/activate 

pip install -e . # edit mode, source code changes reflected in subseuent runs

# run
gardi --debug # enable debug logs
```
